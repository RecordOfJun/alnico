import "../Main.css"
import { MenuBar } from "../../components/MenuBar"

export const Recruitment=()=>{
    return(
        <div className="main_container">
            <MenuBar
            selected_page={12}
            />
        </div>
    );
}